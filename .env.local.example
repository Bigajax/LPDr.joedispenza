# URL do backend (ecobackend888) — usada pelo proxy /api/mp/create-preference
BACKEND_URL=https://ecobackend888.onrender.com

# URL pública desta landing page (sem barra final) — usada como siteUrl nas back_urls do MP
# O backend usa isso para montar: back_urls.success = NEXT_PUBLIC_BASE_URL/dr-joe/obrigado
NEXT_PUBLIC_BASE_URL=https://seudominio.vercel.app
